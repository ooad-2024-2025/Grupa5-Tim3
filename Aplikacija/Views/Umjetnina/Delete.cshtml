@model Grupa5Tim3.Models.Umjetnina

@{
    ViewData["Title"] = "Brisanje umjetnine";
}

<h1 class="mb-4 text-danger">Brisanje umjetnine</h1>

<div class="row">
    <div class="col-md-8">
        <div class="card shadow-sm p-4">
            <h4 class="mb-3 text-muted">Detalji</h4>
            <dl class="row">
                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.naziv)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.naziv)</dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.autor)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.autor)</dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.period)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.period)</dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.datum)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.datum)</dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.tehnika)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.tehnika)</dd>

                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.pocetnaCijena)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.pocetnaCijena)</dd>

                <dt class="col-sm-4">Slika</dt>
                <dd class="col-sm-8">
                    @if (!string.IsNullOrEmpty(Model.SlikaPath))
                    {
                        <img src="@Model.SlikaPath" class="img-thumbnail" style="max-width: 200px;" />
                    }
                    else
                    {
                        <span class="text-muted fst-italic">(Nema slike)</span>
                    }
                </dd>
            </dl>

            @if (User.IsInRole("Administrator"))
            {
                <form asp-action="Delete" class="mt-4">
                    <input type="hidden" asp-for="umjetinaID" />
                    <div class="d-flex align-items-center gap-3">
                        <input type="submit" value="Obriši" class="btn btn-danger" />
                        <a asp-action="Index" class="btn btn-secondary">Nazad</a>
                    </div>
                </form>
            }
            else
            {
                <div class="alert alert-warning mt-3">
                    Nemate dozvolu za brisanje ove umjetnine.
                </div>
                <a asp-action="Index" class="btn btn-secondary mt-2">Nazad</a>
            }
        </div>
    </div>
</div>
